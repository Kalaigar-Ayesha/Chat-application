apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp.fullname" . }}-mongo
spec:
  selector:
    app: mongo
  ports:
    - port: {{ .Values.service.mongoPort }}
      targetPort: {{ .Values.service.mongoPort }}
  type: ClusterIP