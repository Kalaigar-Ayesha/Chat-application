apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "chatapp.fullname" . }}-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.mongo.storage }}